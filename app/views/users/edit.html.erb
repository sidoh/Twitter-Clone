<h1>Edit profile</h1>

<%= form_for(@user) do |f| %>
	<%= render 'shared/error_messages', :o => f.object %>
	<%= render 'form', :f => f %>
<% end %>

<div>
	<%= gravatar_for @user %>
	<a href="http://gravatar.com/emails">change</a>
</div>